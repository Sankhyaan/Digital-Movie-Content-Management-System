The objective of this project is to design and implement a fully normalized relational database system for an OTT content management platform, similar to Netflix. The system manages movies, series, seasons,
episodes, actors, genres, languages, and OTT platform availability using MySQL. The project demonstrates realworld application of DDL, DML, stored procedures, user-defined functions, triggers, cursors, transactions, views,
and indexing.
